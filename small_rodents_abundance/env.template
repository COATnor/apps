# Copy to .env and fill in the values. .env is never committed.
# COAT data portal API key (allows access to non-public datasets).
COAT_API=
# COAT data portal URL (optional, defaults to https://data.coat.no in the Dockerfile).
# COAT_URL=https://data.coat.no
